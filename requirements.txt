# MultiTab requirements
# Python 3.12 + CUDA 12.9 (RTX 5000 Ada) 환경 기준
#
# 설치 방법:
#   1) pip install torch --index-url https://download.pytorch.org/whl/cu128
#   2) pip install -r requirements.txt

# ── HPO ────────────────────────────────────────────────────────────────────
optuna>=3.5.0

# ── 딥러닝 (위 설치 방법 1번으로 별도 설치) ───────────────────────────────
# torch==2.0.1 은 CUDA 12.x 환경 pip 미지원 → 최신 버전 사용
torch>=2.2.0

# ── 데이터 처리 ─────────────────────────────────────────────────────────────
# scikit-learn 1.2.2는 Python 3.12에서 빌드 오류 → 1.3.0 이상 필요
scikit-learn>=1.3.0
# scipy 1.10.1은 numpy 최신 버전과 충돌 → 1.11.0 이상 필요
scipy>=1.11.0
numpy>=1.26.0
pandas>=2.1.0

# ── 트리 모델 ───────────────────────────────────────────────────────────────
xgboost>=1.7.5
catboost>=1.2
lightgbm>=3.3.5

# ── TabPFN (libs/tabpfn.py 의존성) ─────────────────────────────────────────
tabpfn

# ── TabM (libs/tabm_model.py 의존성) ───────────────────────────────────────
tabm

# ── 기타 ────────────────────────────────────────────────────────────────────
openml>=0.14.1
joblib>=1.2.0
tqdm>=4.66.0
# argparse는 Python 표준 라이브러리 내장 → 별도 설치 불필요